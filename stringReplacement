# place commands within a function (was not necessary but followed along with the project)
# create a string
# create two string variables initialized with user input
# use the str.replace function along with the two variables
# call the function


def replace_word():
    
    str = "hi guys, I am Talor, and hi hi hi hi"
    word_to_replace = input("Enter the word to replace: ")
    word_replacement = input("Enter the world replacement: ")
    print(str.replace(word_to_replace, word_replacement))
    
replace_word()
